[package]
name = "test_harness_sqisign"
version = "0.1.0"
edition = "2024"

[dependencies]
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
chrono = "0.4"
libc = "0.2"

[build-dependencies]
cmake = "0.1"
